[tox]
envlist = pypy,py{27,35}

[testenv]
deps = -rtest_requirements.txt

# To make sure we actually test the tarball and now the working directory
changedir = .tox
commands=python {toxinidir}/test_apiblueprint.py
passenv = *
